import { TabEnablerDirective } from './tab-enabler.directive';

describe('TabEnablerDirective', () => {
  it('should create an instance', () => {
    const directive = new TabEnablerDirective();
    expect(directive).toBeTruthy();
  });
});
