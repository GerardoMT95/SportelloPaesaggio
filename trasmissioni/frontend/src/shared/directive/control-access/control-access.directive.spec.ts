import { ControlAccessDirective } from './control-access.directive';

describe('ControlAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlAccessDirective();
    expect(directive).toBeTruthy();
  });
});
